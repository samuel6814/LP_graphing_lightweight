# Phase 2 — Global Tools

## Acceptance criteria

- [x] `ToolsContext` for shared graph/calc/solver state
- [x] `GraphingPanel` with multi-expression cards + SVG canvas
- [x] `CalculatorPanel` scientific keypad
- [x] `StepSolverPanel` step timeline
- [x] `ToolsDock` FAB + slide-over on learn routes
- [x] Topic pages pre-seed graph from `defaultGraph`
